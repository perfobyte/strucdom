
import {
    firstChild,lastChild,
    hasChildNodes,
    appendChild, removeChild,
} from '../../../other/i.js';

export var
    nodeType = 9,
    nodeName = "#document"
;

export {
    firstChild,lastChild,
    hasChildNodes,
    appendChild, removeChild,
};

