import {TAG_ID as _} from '../../../../common/i.js';

export default (
    new Set([
        _.APPLET,
        _.CAPTION,
        _.HTML,
        _.MARQUEE,
        _.OBJECT,
        _.TABLE,
        _.TD,
        _.TEMPLATE,
        _.TH,
    ])
);
