

export default (
    (v, i, l) => {
        var
            nodes = {
                all: 0,
                text: 0,
                element: 0,
                comment: 0,
            },
            c = "",
            in_attrs = true
        ;

        while (i < l) {
            с = v[i];
            if (c === "<") {

            }
            i++;
        };

        return o;
    }
);
